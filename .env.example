# OpenCode Telegram Bot Configuration
# Copy this to .env and fill in your values

# Required: Telegram bot token from @BotFather
TELEGRAM_BOT_TOKEN=your-telegram-bot-token-here

# Required: Comma-separated list of Telegram user IDs allowed to use the bot
# Get your ID from @userinfobot on Telegram
TELEGRAM_ALLOWED_USER_IDS=123456789,987654321

# Optional: Path to opencode CLI (default: opencode)
# OPENCODE_PATH=/usr/local/bin/opencode

# Optional: Default working directory for new sessions (default: ~)
# OPENCODE_DEFAULT_CWD=/home/user/projects

# Optional: Log file path (default: ./bot.log)
# OPENCODE_LOG_FILE=./bot.log

# Optional: Sessions file path (default: ./sessions.json)
# OPENCODE_SESSIONS_FILE=./sessions.json

# Optional: OpenCode server daemon URL for faster response times (Phase 2+3)
# When running `opencode serve --port 8080`, the bot will use HTTP API for faster requests
# OPENCODE_SERVER_URL=http://127.0.0.1:8080

# Optional: Model provider for API mode (default: anthropic)
# OPENCODE_MODEL_PROVIDER=anthropic

# Optional: Model ID for API mode (default: claude-sonnet-4-20250514)
# OPENCODE_MODEL=claude-sonnet-4-20250514

# OpenCode uses provider-specific API keys. Configure in ~/.config/opencode/config.json
# or via environment variables like:
# ANTHROPIC_API_KEY=your-anthropic-api-key
# OPENAI_API_KEY=your-openai-api-key
